{
  "name": "northstar-priority-matrix",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node support/check-syntax.mjs && node --test",
    "sbom": "sh support/generate-sbom.sh"
  }
}
